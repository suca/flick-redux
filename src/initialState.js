/**
 * initialState
 *
 * It defaults the initial state
 */
const initialState = {
    tags: [],
    isLoading: false,
    isError: false,
    photosData: [],
    maxItemsPerCall: 10,
};

export default initialState;
