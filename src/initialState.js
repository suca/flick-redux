/**
 * initialState
 *
 * It defaults the initial state
 */
const initialState = {
    tags: [],
    isLoading: false,
    isError: false,
    photosData: [],
    maxItemsPerCall: 20,
};

export default initialState;
